export type Student = {
    id: string;
    name: string;
    degree?: string;
    email?: string;
};

export type ExperienceType = {
    name: string;
};

export type ProjectType = {
    title: string;
    category: string;
};